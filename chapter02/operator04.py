a = 10  # 기본 할당

# 복합 할당 연산자
a += 5   # a = a + 5와 동일 (a는 15)
a -= 3   # a = a - 3와 동일 (a는 12)
a *= 2   # a = a * 2와 동일 (a는 24)
a /= 4   # a = a / 4와 동일 (a는 6.0)
a //= 2  # a = a // 2와 동일 (a는 3.0)
a %= 2   # a = a % 2와 동일 (a는 1.0)
a **= 3  # a = a ** 3와 동일 (a는 1.0)